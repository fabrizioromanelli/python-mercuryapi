echo User mode tests

# XXX really just tests paramater parsing, printing, and value persistence

# Check default
get /reader/userMode

set /reader/userMode PRINTER
get /reader/userMode

set /reader/userMode PORTAL
get /reader/userMode

set /reader/userMode NONE
get /reader/userMode

# Bogus
set /reader/userMode ABCDE
set /reader/userMode NULL

